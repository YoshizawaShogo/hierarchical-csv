# 実装中に見つけた疑問・要検討事項（issue ためおき）

> 実装しながら気づいた「設計で未確定 / 要判断 / 要相談」をここに貯める。
> 解決したら決定ログ（docs/design.md 第2章）へ反映し、ここからは消すか「解決済み」に。

## 環境

- **I-ENV-1**: この実行環境に `webkit2gtk-4.1` が無く、**フル `tauri build`（GUI 起動）は不可**。
  そのためドメイン中核を Tauri 非依存の `core/` crate に分離し `cargo test` で検証する方針にした（D-038）。
  GUI の実起動確認は webkit のある環境が必要。

## 仕様の詰め残し（実装中に判断が要る）

- **I-VAL-1**: `type=int` / `float` の検証仕様。空欄の扱い（`required=false` なら空欄OK?）、
  `int` に `"12.5"` や前後空白、符号、桁区切りは? 現状 core は「trim 後、Rust の `i64`/`f64` パース成功で合格、空欄は `required` に委ねる」で暫定実装。
- **I-VAL-2**: `enum` の一致は**大文字小文字を区別**する前提で暫定実装。区別なしオプションが要るか要検討。
- **I-VAL-3**: `pattern` / `when` の `matches` は**全体一致**（`^...$` を暗黙付与せず、`regex::is_match` の部分一致）か、
  アンカー必須か。暫定では **`regex` の `is_match`（部分一致）**。設計意図の確認要。
- **I-WHEN-1**: `when` 条件値のクォート。値に空白・カンマ・`}` を含む場合の記法未定。
  暫定パーサは `{key=value, ...}` を単純分割し、値は前後 trim のみ（クォート非対応）。
- **I-WHEN-2**: `in=[a,b,c]` のリスト記法。暫定で `[` `]` 内をカンマ分割・trim。空要素やクォートは未対応。
- **I-DIFF-1**: セル単位差分アルゴリズム（`git_diff`）。行の挿入/削除時の行アラインメントが未設計。
  現状 `git.rs` は `status/add/commit/log` を実装、`diff_cells` は**未実装スタブ**。最難関につき要要件詰め。
- **I-COL-1**: 列の追加/リネーム/削除の操作と、列名キーの制約・スタイルの追従が未設計。
- **I-TRUST-1**: 信頼プロンプトの発火タイミングと信頼状態の保存先が未定（フック/コード実行のガード, D-027）。
- **I-OPEN-1**: プロジェクトを開く UX（ルート選択・最近使った一覧）が未定。
- **I-STYLE-1**: `Attr` の正式な属性セット（配置・列幅・行高 等）と描画へのマッピングが暫定。

## フロントエンド

- **I-FE-1**: フロントは骨組みのみ（左サイドバー切替＋LibreOffice 風グリッドのレイアウト雛形）。
  Tauri コマンド呼び出しは webkit 環境が無いため未疎通。実データ結線は GUI 環境で要確認。
